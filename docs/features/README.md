# Features

1. A frontend UI for interaction
2. A backend support system, handling:
    - Configuration
    - Request sending
    - Message receiving

Frontend UI subscribes to a state pool, which is changed by the backend.
